// Theme switcher removed — app uses light mode only now.
// Kept as a null-renderer so existing imports don't break.
import React from 'react';

export default function ThemeSettings() {
  return null;
}
