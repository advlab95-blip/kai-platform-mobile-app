// Language switcher removed — app is Arabic-only now.
// Kept as a null-renderer so existing imports don't break.
import React from 'react';

export default function LanguageSettings() {
  return null;
}
